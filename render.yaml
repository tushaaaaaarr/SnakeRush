services:
  - type: web
    name: snakerush-backend
    runtime: python
    runtimeVersion: 3.11.10
    buildCommand: cd backend && pip install --only-binary :all: -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT

